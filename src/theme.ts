import MinUi from "./core/MinUi";

MinUi.Css.Dark = {
  chart: {
    navbar: "#ff00ff",
  },
};

MinUi.Css.Light = {
  chart: {
    borders: {
      input: "#ff00ff",
      button: "#d3d3d3",
    },
  },
};
