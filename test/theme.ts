import { SetTheme } from "../src/core/MinUi";

SetTheme({
  chart: {
    borders: {
      input: "#ff00ff",
      button: "#d3d3d3",
    },
    button: {
      main: "orange",
      textContrasts: "black",
    },
  },
});
